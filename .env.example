# Required for the README vignette and smoke-test configs.
ANTHROPIC_API_KEY=

# Optional: used by configs that route through OpenAI-compatible models or proxies.
OPENAI_API_KEY=

# Optional: higher-throughput Semantic Scholar access for retrieval-heavy runs.
S2_API_KEY=

# Optional: local OpenAI-compatible subagent endpoint.
# Only used if your chosen config also points the subagent model at that endpoint.
LLM_BASE_URL=